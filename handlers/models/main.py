from utils.config import ADMIN_ID

from database import DatabaseManager

from pyrogram import enums, Client, filters

def register_all_ai_handlers(app: Client, db: DatabaseManager, log_bot, log_warn):
    pass